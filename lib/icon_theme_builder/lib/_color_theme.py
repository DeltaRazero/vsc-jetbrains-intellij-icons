
class __:

    import enum

# *****************************************************************************

class ColorTheme (__.enum.Enum):
    DEFAULT_DARK = __.enum.auto(),
    LIGHT        = __.enum.auto(),
    CONTRAST     = __.enum.auto(),
